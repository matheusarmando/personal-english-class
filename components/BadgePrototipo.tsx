export default function BadgePrototipo() {
  return (
    <div className="max-w-2xl mb-6 rounded-xl border border-warn/40 bg-warn/10 p-3 text-xs text-ink/70">
      <span className="font-semibold text-warn">Protótipo:</span> os
      alunos aqui são reais, mas o conteúdo desta tela ainda não é salvo
      no banco de dados — some ao atualizar a página.
    </div>
  );
}
